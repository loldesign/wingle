<%= simple_form_for [:admin, @state, @city, @city_locale] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@city_locale.persisted? ? 'Editar' : 'Criar'} Bairro" %></h2>
    </header>
    
    <article>
      <%= f.input :name, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_state_city_city_locales_path(@state, @city)) %>
    <%= submit_button f %>
  </footer>
<% end %>
