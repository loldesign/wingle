<%= title_page 'Termos' %>

<%= add_resource 'Novo Termo', new_admin_term_path %>

<table class='table-page'>
  <thead>
    <tr>
      <th>Título</th>
      <th>Termo</th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <tbody>
    <%= display_list_for(@terms, fields: 3) %>
  </tbody>
</table>

<%= will_paginate @terms %>
