<%= simple_form_for [:admin, @relevance] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@relevance.persisted? ? 'Editar' : 'Criar'} Relevância" %></h2>
    </header>
    
    <article>
      <%= f.input :name, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_relevances_path) %>
    <%= submit_button f %>
  </footer>
<% end %>
