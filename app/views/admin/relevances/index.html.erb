<%= title_page 'Relevâncias' %>

<%= add_resource 'Nova Relevância', new_admin_relevance_path %>

<table class='table-page'>
  <thead>
    <tr>
      <th>Relevância</th>
      <th>Criado em</th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <tbody>
    <%= display_list_for(@relevances, fields: 3) %>
  </tbody>
</table>

<%= will_paginate @relevances %>
