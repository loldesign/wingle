<%= simple_form_for [:admin, @nofsalaries_list] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@nofsalaries_list.persisted? ? 'Editar' : 'Criar'} Nofsalaries" %></h2>
    </header>
    
    <article>
      <%= f.input :name, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_nofsalaries_lists_path) %>
    <%= submit_button f %>
  </footer>
<% end %>
