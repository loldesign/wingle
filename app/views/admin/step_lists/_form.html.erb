<%= simple_form_for [:admin, @step_list] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@step_list.persisted? ? 'Editar' : 'Criar'} Etapa" %></h2>
    </header>
    
    <article>
      <%= f.input :name, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_step_lists_path) %>
    <%= submit_button f %>
  </footer>
<% end %>
