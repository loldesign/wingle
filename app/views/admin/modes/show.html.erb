<%= title_page 'Modes' %>

<section class="fields-wrapper two-columns margin-top">
  <article>
    <p>
      <strong>Nome:</strong> <%= @mode.name %>
    </p>

    <p>
      <strong>Descrição:</strong> <%= @mode.description %>
    </p>
  </article>
</section>