<%= title_page 'Áreas' %>

<%= add_resource 'Nova Área', new_admin_area_path %>

<table class='table-page'>
  <thead>
    <tr>
      <th>Área</th>
      <th>Criado em</th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <tbody>
    <%= display_list_for(@areas, fields: 3) %>
  </tbody>
</table>

<%= will_paginate @areas %>
