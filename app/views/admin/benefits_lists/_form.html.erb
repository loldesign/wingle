<%= simple_form_for [:admin, @benefits_list] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@benefits_list.persisted? ? 'Editar' : 'Criar'} Benefício" %></h2>
    </header>
    
    <article>
      <%= f.input :name, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_benefits_lists_path) %>
    <%= submit_button f %>
  </footer>
<% end %>
