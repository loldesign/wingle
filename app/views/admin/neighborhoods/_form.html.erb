<%= simple_form_for [:admin, @state, @city, @city_locale, @neighborhood_group, @neighborhood] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@neighborhood.persisted? ? 'Editar' : 'Criar'} Bairro" %></h2>
    </header>
    
    <article>
      <%= f.input :code, wrapper_html: {class: 'col-2' } %>
      <%= f.input :name, wrapper_html: {class: 'col-10' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_state_city_city_locale_neighborhood_group_neighborhoods_path(@state, @city, @city_locale)) %>
    <%= submit_button f %>
  </footer>
<% end %>
