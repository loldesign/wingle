<%= simple_form_for [:admin, @position] , html: {class: 'form'} do |f| %>

  <section class='fields-wrapper one-column'>
    <header>
      <h2><%= "#{@position.persisted? ? 'Editar' : 'Criar'} Cargo" %></h2>
    </header>
    
    <article>
      <%= f.input :position, wrapper_html: {class: 'col-12' } %>
    </article>
  </section>
  <footer>
    <%= cancel_action(path: admin_positions_path) %>
    <%= submit_button f %>
  </footer>
<% end %>
