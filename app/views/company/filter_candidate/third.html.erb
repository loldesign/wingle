<section id="third_step" class="filter-container content process-selection-options">
  <section class="content-box">
    <ul class="collection with-header">
      <li class="collection-header">
        <div class="progress">
          <div class="determinate" style="width: 60%"></div>
        </div>
        <h5 class="center-align section">Título</h5>
      </li>

      <%= form_tag company_filter_step_4_path do %>
        <% @title_list.each do |title_list| %>
          <li class="collection-item">
            <%= link_to title_list.name, 'javascript://', data: {id: title_list.id}, class: 'option' %>
          </li>
        <% end %>

        <%= hidden_field_tag 'process_selection[title_list_id]' %>
      <% end %>
    </ul>
  </section>

  <section class="action-buttons-single">
    <%= link_to "Anterior", company_filter_step_2_path, class: "waves-effect waves-light btn-large" %>
  </section>
</section>
